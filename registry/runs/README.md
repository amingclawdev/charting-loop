# Legacy v1 run records

Only immutable `charting-loop/registry-entry/v1` JSON records belong here. This path is
maintained for v1 compatibility; new neutral experiments use
`exogenous/runs/<run-id>/`.
