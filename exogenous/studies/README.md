# Studies

Each `studies/<study-id>/STUDY.json` freezes its TASKSET join, comparison intent,
conditions, protocol digest, and assignment rule. A method treatment must point to an
eligible cataloged method version. Draft v2 is `study_eligible=true` and may be pinned
for study; it remains `adoption_eligible=false` and is not operational approval.
