# Legacy reference task

These bytes belong to the pilot v1 evaluation fixture and remain validated by
`reference-task/VERSIONS.json`. They are not the neutral task registry.

New task identities belong in `exogenous/benchmarks/<benchmark>/<release>/TASKSET.json`
and are joined through STUDY and RUN manifests. Do not infer exact same-task comparison
from this legacy S1–S6 protocol alone.
