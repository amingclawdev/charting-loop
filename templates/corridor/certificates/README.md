# Path certificates

A path certificate is an independent judgment over a frozen Entrance and traversal
receipt. It cannot be inferred from a passing task score, an authority log, or the
Candidate's own statement.

A passing certificate makes only its witnessed path provisionally charted. Current C is
scope-indexed and additionally requires a live EvidentialWarranty over that scope.
