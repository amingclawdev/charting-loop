# Method paper

`METHOD.md` is the sole normative method source. `SCOPE-DATUM.md`, `OUTLINE.md`,
`VERSIONS.json`, and `references.bib` record scope, navigation, version identity, and
bibliography; they do not add method requirements.

The service-plan example linked from `METHOD.md` is a completed, non-normative
engineering demo. It demonstrates one declared runtime-evidence path without unlocking
the method for operational adoption or establishing effectiveness. The Aming Claw
Contract-handoff paragraph is one non-normative projection; it is not a reference
architecture or evidence that the named system satisfies the method.

Within the method, Rule and Fact are input planes and Guide is the deterministic control
plane that returns one Entrance or typed refusal. Papers and packages are MethodRefs or
knowledge inputs; they are not Guide merely because they advise a builder.

The catalog retains more than one independently frozen, study-eligible MethodRef.
Historical studies may continue to pin `paper2-current-v2`, frozen at
`f58e065ad5d035b55fc8668b094e42327382345a`. At that exact public-history commit,
`METHOD.md` is
`sha256:ce9c94c12660c57a8dc34c98afd46c644b07214a2f9e7a9a494078fbe7be0905`
and `SCOPE-DATUM.md` is
`sha256:c0764df57d20b5d66500ec75c8f8fcd5e0dd5c5029bd35783caca6a68133e307`.
The v4 benchmark package is `charting-loop-method-v4`, frozen at
`0d3ed5c357c906edcc697a83b3ce681c68cd353a`, with METHOD digest
`sha256:d3a9da497c31f3bde46a31f37990236af51b9f677ae807d023582b27254c4ab0`
and SCOPE-DATUM digest
`sha256:65c6a91120c15bec30278288a26ecc98bdf96cfb07fd490dc915408a78844327`.
Both frozen packages are `study_eligible=true`, `adoption_eligible=false`, and
`builder_eligible=false`; a study must name one exact version and may not request
“current” or “latest.” The prior `draft-v2` package remains non-study-eligible audit
history under its own exact commit and digests. `frozen` records byte identity for
research; it does not authorize operational adoption.
Repository templates and protocol pages are projections. When they conflict with the
cataloged method version, the method source wins and the projection must be corrected.
